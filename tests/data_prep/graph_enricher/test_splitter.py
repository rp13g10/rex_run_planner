"""Check that splitting of graph into subgraphs works as expected, and
that the input graph can be split & reconstructed ad-infinitum"""

from rex_run_planner.data_prep.graph_enricher.splitter import GraphSplitter
